server:
  port: 8888

spring:
  application:
    name: configuration-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/devwuu/spring-cloud-study.git
          default-label: section/configuration
